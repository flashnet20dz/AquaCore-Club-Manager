# ═══════════════════════════════════════════════════════════
# manual-sql-postgres — سكربتات PostgreSQL يدوية سابقة (محفوظة للتاريخ)
# ═══════════════════════════════════════════════════════════
# هذه السكربتات كانت سابقاً داخل prisma/migrations/ لكنها بلغة PostgreSQL
# (كُتبت يدوياً لتشغيلها على Neon قبل إرساء نظام Migrations) — وضعها داخل
# سلسلة ترحيلات SQLite يكسر prisma migrate deploy من الصفر.
# الترتيب الزمني للتطبيق على إنتاج PG كان:
#   1) 20260711000000_add_employees_contracts
#   2) 20260717200000_add_compensation_and_sync
# baseline_postgres_full.sql = إعادة بناء كاملة من الصفر (أحدث من الاثنين)
