# 🚀 دليل رفع تطبيق AquaCore على Google Play والربط الهجين

هذا الدليل يشرح بالتفصيل كيفية تحضير ورفع تطبيق **AquaCore Club Manager** على متجر **Google Play Store**، وكيفية ربط الهاتف والكمبيوتر بنفس قاعدة البيانات (سحابياً أو محلياً)، وطريقة التفعيل أوفلاين.

---

## 📱 أولاً: متطلبات وتجهيزات متجر Google Play

منذ عام 2021، تشترط شركة Google رفع التطبيقات بصيغة **Android App Bundle (.aab)** بدلاً من صيغة .apk التقليدية، مع استهداف أحدث إصدار أندرويد (**Target SDK 34**).

### معلومات التطبيق المهيأة في المشروع:
- **اسم التطبيق (App Name):** AquaCore Club Manager
- **معرّف الحزمة (Package ID):** `com.aquacore.clubmanager`
- **الإصدار (Version):** 1.0.0 (Version Code: 1)
- **Target SDK:** 34 (متوافق مع أندرويد 14 و 15 ومتطلبات Google Play 2026)
- **Min SDK:** 22 (يدعم أكثر من 99% من أجهزة الأندرويد في السوق)
- **الأيقونات والشاشات الترحيبية:** جاهزة ومدمجة لجميع دقات الشاشات (`mdpi` إلى `xxxhdpi`)

---

## 🛠️ ثانياً: كيفية استخراج حزمة Google Play (.aab) و (.apk)

### الطريقة 1: تلقائياً عبر GitHub Actions (الأسهل والموصى بها)
بمجرد رفع التحديثات إلى المستودع على GitHub:
1. اذهب إلى تبويب **Actions** في مستودع المشروع:
   `https://github.com/flashnet20dz/AquaCore-Club-Manager/actions`
2. اختر آخر عملية بناء: **Build Android (APK & Google Play Bundle)**
3. عند اكتمال البناء الأخضر ✅، ستجد في قسم **Artifacts** في الأسفل ملفاً مضغوطاً يحتوي على:
   - **`AquaCore-Club-Manager.aab`** ⭐️ (هذا الملف مخصص للرفع على Google Play Console)
   - **`AquaCore-Club-Manager.apk`** 📱 (هذا الملف مخصص لتثبيته مباشرة وتجربته على أي هاتف)
4. كما يتم تلقائياً إنشاء **Release** رسمي يحمل روابط تحميل مباشرة.

### الطريقة 2: محلياً من جهاز الكمبيوتر
إذا كان لديك بيئة Android SDK و Java 17 مثبتة:
- انقر نقراً مزدوجاً على ملف: **`scripts/build-android.bat`**
- أو شغّل في PowerShell:
```powershell
.\scripts\build-android.ps1
```
ستجد الملفات الناتجة في مجلد `output/`.

---

## 🌐 ثالثاً: ربط تطبيق الهاتف والكمبيوتر بنفس قاعدة البيانات

يدعم النظام وضعين للربط:

### 1. الوضع السحابي (عبر الإنترنت) — Cloud Mode
- قاعدة البيانات المركزية: **PostgreSQL** (على Neon أو Supabase).
- رابط الخادم الافتراضي: `https://aladine-pool-manager.vercel.app`
- **النتيجة:** أي تعديل في الهاتف أو الكمبيوتر يظهر فوراً في كلا الجهازين في نفس اللحظة من أي مكان في العالم.

### 2. الوضع المحلي (الشبكة المحلية / الواي فاي بدون إنترنت) — Local LAN Mode
إذا لم يكن هناك إنترنت في النادي:
1. شغّل السيرفر على الكمبيوتر عبر ملف **`start-server.bat`** (يعمل بقاعدة بيانات SQLite محلية).
2. اتصل بالهاتف والكمبيوتر على **نفس شبكة الواي فاي** (WiFi الراوتر حتى بدون نت).
3. في تطبيق الهاتف:
   - اضغط على زر **"ربط الشبكة والسيرفر"** أو افتح نافذة الإعدادات.
   - اختر **"السيرفر المحلي (WiFi)"** واكتب عنوان IP الكمبيوتر (مثال: `http://192.168.1.15:3000`).
   - اضغط **"فحص الاتصال"** ثم **"حفظ والاتصال"**.
4. **النتيجة:** الهاتف والكمبيوتر يقرآن ويكتبان في **نفس قاعدة بيانات SQLite المحلية** بدون استهلاك أي ميغابايت إنترنت.

---

## 🔑 رابعاً: طريقة تفعيل الاشتراك محلياً (أوفلاين بدون إنترنت)

إذا كان النادي في منطقة بدون إنترنت أو منشأة مغلقة، يمكنك تفعيل الاشتراك 100% بدون إنترنت:

### 1. توليد كود التفعيل من السوبر أدمن (SuperAdmin)
- يدخل السوبر أدمن إلى لوحة التحكم `super-admin/` -> **أكواد التفعيل**.
- يختار خطة الاشتراك (شهر، 3 أشهر، 6 أشهر، سنة، سنتان) ويولّد الكود (صيغته: `AQCR-Y1-XXXXXXXX-XXXX`).
- هذا الكود يحمل توقيعاً رياضياً مشفراً بـ **HMAC-SHA256**.

### 2. التفعيل في النادي (بدون إنترنت)
1. في نسخة النادي (تطبيق الكمبيوتر أو الهاتف):
2. افتح نافذة **"تفعيل كود ترخيص المنظومة"**.
3. اكتب الكود واضغط **تفعيل**.
4. **ماذا يحدث في الخلفية؟**
   - يتحقق النظام رياضياً من صحة التوقيع المشفر أوفلاين.
   - ينشئ السجل في قاعدة البيانات المحلية ويمنح النادي رخصة سارية لكامل المدة.
   - يتم تخزين شهادة الرخصة محلياً ويفتح النظام فوراً لجميع المستخدمين.
   - عند توفر الإنترنت لاحقاً، تتم مزامنة العملية تلقائياً مع السحابة لتسجيل الكود كمستخدم.

---

## 📋 خامساً: خطوات الرفع على Google Play Console

1. افتح حساب مطور في [Google Play Console](https://play.google.com/console).
2. اضغط **Create app** (إنشاء تطبيق):
   - **App name:** AquaCore Club Manager
   - **Default language:** العربية (ar)
   - **App or game:** App
   - **Free or paid:** مجاني أو مدفوع حسب رغبتك.
3. في القائمة الجانبية -> قسم **App content** (محتوى التطبيق):
   - **Privacy Policy (سياسة الخصوصية):** ضع رابط سياسة الخصوصية.
   - **App access (الوصول للتطبيق):** ضع حساب تجريبي لفريق المراجعة (مثال: مستخدم بصلاحية المدير).
   - **Target audience:** 18+ (أو مدراء الأندية الرياضية).
4. في القائمة الجانبية -> **Production** (أو **Internal testing** للاختبار الداخلي):
   - اضغط **Create new release**.
   - ارفع ملف: **`output/AquaCore-Club-Manager.aab`**.
   - اكتب ملاحظات الإصدار (Release notes): "الإصدار الأول لمنظومة إدارة الأندية الرياضية والاشتراكات".
   - اضغط **Next** ثم **Save** و **Start rollout**.
5. بعد مراجعة Google (عادة 1-3 أيام)، يصبح تطبيقك متاحاً رسمياً على متجر Google Play!

---

© 2026 AquaCore Club Manager — جميع الحقوق محفوظة
